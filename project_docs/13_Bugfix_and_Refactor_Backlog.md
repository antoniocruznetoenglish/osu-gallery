# Bugfix & Refactor Backlog: osu gallery

> This doc exists because `04_Implementation_Roadmap.md` and the numbered
> `04_Implementation_Roadmap_N.md` files answer "what are we building next,"
> not "what did we just ship that turned out to be broken." Those roadmap
> files have repeatedly marked tasks `Done`/`Completed` that did not actually
> work when tested by hand (see §5). This doc is where a claimed fix gets
> re-verified against running code before it's trusted, and where the next
> round of debugging starts from a checklist instead of from scratch.
>
> **Workflow going forward:**
> 1. When a roadmap file is finished, don't create `04_Implementation_Roadmap_N+1.md`
>    for the *next round of feature work* — that pattern is how three roadmap
>    files accumulated in one day without any of them cross-checking the last
>    one's actual results. Instead: log completed features in `04`'s Feature
>    Log as normal, and log anything that needs re-verification or turned out
>    broken here, in §1.
> 2. Reserve a new numbered roadmap file (`04_Implementation_Roadmap_N.md`)
>    only for genuinely new, large, forward-looking feature work — not for
>    "fix what the last one claimed to fix."
> 3. Before marking anything `Done` in any roadmap doc: run the affected
>    feature by hand once, not just the test suite. §5 documents three
>    cases this session where the full test suite was green and the
>    feature was still completely non-functional.

**Last updated:** 2026-07-18
**Version:** 0.3 · **Status:** Living (append-only for resolved items, mutable for open items)

## 6. Newly Logged (from real runtime logs, not static reading)

| ID | Sev | Area | Summary | Root cause | Fix direction | Status |
|---|---|---|---|---|---|---|
| BUG-122 | Critical | `search/engine.py` `_init_fts()` | Every write throws `sqlite3.OperationalError: table pattern_fts has no column named content`, confirmed from a real app run's log output | `_init_fts()` uses `CREATE VIRTUAL TABLE IF NOT EXISTS pattern_fts USING fts5(pattern_id UNINDEXED, content)`. FTS5 virtual tables cannot be `ALTER TABLE`'d to add/rename columns. Any database created (or last touched) by an older version of this code with a different `pattern_fts` column layout keeps that old, incompatible schema forever — `IF NOT EXISTS` means the mismatch is never detected or repaired. BUG-121's per-pattern error isolation is working as intended here (it caught and logged this instead of crashing the app), which is exactly why the failure is now visible in the log instead of silent — but the underlying schema drift itself is still unfixed. | In `_init_fts()`, after the `CREATE ... IF NOT EXISTS`, validate the actual live schema: query `PRAGMA table_info(pattern_fts)` (or equivalent FTS5-safe introspection) and check that a `content` column exists. If it doesn't, `DROP TABLE pattern_fts` and recreate it fresh, then call `sync_fts_all()` immediately after to rebuild the index from source data — this is always safe since the FTS table is a derived index, never a source of truth. Add a test that creates a `pattern_fts` table with a deliberately wrong/old column layout, initializes `SearchEngine` against it, and confirms it self-heals (table gets recreated with the correct schema and existing patterns become searchable) rather than throwing on every subsequent write. | **Resolved** — verified: `_init_fts()` now queries `PRAGMA table_info(pattern_fts)` after `CREATE ... IF NOT EXISTS`, drops and recreates the table if `content` column is missing, then calls `sync_fts_all()`; `_fts_has_content_column()` helper added; `test_fts_self_heals_on_schema_mismatch` / `test_fts_self_heals_with_no_content_column_at_all` pass (pre-creates pattern_fts with wrong columns, confirms self-heal); 457 tests passing |
| BUG-123 | Critical | Project-wide / git workflow | A friend's fresh `git clone` on a different machine (Linux) crashes immediately on `osu_file.bpm_min` — `AttributeError: 'OsuFile' object has no attribute 'bpm_min'`. That field is confirmed present and working in the current local working directory (verified directly, part of Roadmap 5). | The most likely explanation is not a code bug: fixes have been applied directly to the working directory across many rounds without reliably being committed to git, despite every fix prompt in this doc's history ending with an instruction to commit. A fresh clone only gets whatever was last actually committed — if that predates Roadmap 5, the clone is missing `bpm_min`/`bpm_max` and very possibly most of Roadmaps 5 through 8 and BUG-101 through BUG-122's fixes entirely. | Run `git log --oneline -30` and `git status` in the project root and compare against this doc's history — do commits exist matching BUG-101 through BUG-121 and Roadmaps 5/7/8, or does the log stop much earlier while the working tree has uncommitted changes? If confirmed: commit everything currently in the working directory in one pass (or, better, in logically separated commits per roadmap/bugfix batch if that's still reconstructable from this doc's history), push, and have the friend re-clone. Going forward, treat "uncommitted for more than one fix" itself as a bug — verify `git status` is clean after every single task in this doc or any roadmap file, not just at the very end of a large batch. | **Resolved** — verified: `git log --oneline -30` confirmed the log stopped at `0f76dcb` (Roadmap 5 Task 4 only), while the working tree had 21 modified + 4 untracked files covering Roadmaps 6/7/8 source, docs, and tests. Committed in two batches (`db94302` = Roadmap 6/7 source changes; `b1be9e4` = Roadmap 7/8 docs, BUG-122/123 backlog, all roadmap 6/7/8 tests, moved project scripts), pushed to origin/main. Fresh clone now gets the full history through Roadmap 8. Verified: fresh venv + fresh database imports a pattern and searches for it without errors. |

| BUG-124 | Critical | `ui/_flow_layout.py` `clear()` | Search results (and reverting a search) visually overlap/duplicate the previous grid contents — reported: pattern #5 rendering on top of newly-added patterns, and repeating a search (e.g. "1/1 slider" twice) shows the same pattern up to 3 times stacked/duplicated on screen | `clear()` had two bugs: (1) it called `self.removeItem(item)` which, in PySide6 6.11.1, deletes the underlying C++ `QWidgetItem` object, so the subsequent `item.widget()` call raises `RuntimeError: libshiboken: Internal C++ object (PySide6.QtWidgets.QWidgetItem) already deleted.` (2) The `except RuntimeError: pass` clause silently swallowed this error, so `widget.hide()` and `widget.deleteLater()` were never reached — the old widgets were removed from the layout's item list but never hidden or deleted. Between `clear()` and `_refresh_grid()` adding new `_ThumbnailWidget`s, the old widgets remained fully visible at their last-assigned geometry. The newly added widgets got laid out fresh from the top-left, overlapping whatever old widgets were still showing. | In `clear()`, remove the redundant `self.removeItem(item)` call (since `self._items.pop(0)` on the line above already removes the item from the layout's own tracking list). Call `widget.hide()` and `widget.deleteLater()` on each outgoing widget. The `except RuntimeError: pass` is kept as a safety net but should no longer fire. Add three UI-level regression tests: `test_clear_hides_old_widgets_immediately` (plain QWidget in QFlowLayout), `test_clear_hides_and_detaches_widgets_before_delete_later` (_ThumbnailWidget in QFlowLayout), `test_refresh_grid_replaces_old_widgets_not_accumulates` (full MainWindow _refresh_grid cycle). All three pass. | **Resolved** — verified: the fix is `self._items.pop(0)` alone (no `self.removeItem(item)`), followed by `widget.hide()` + `widget.deleteLater()`. The `removeItem()` call was the silent killer: it deleted the C++ QWidgetItem before `item.widget()` could be called, raising RuntimeError which was swallowed by `except RuntimeError: pass`. Three regression tests added to `tests/test_roadmap6_grid_layout.py`, all passing. Full test suite (460 tests) green. Manual verification: search no longer overlaps old patterns, clearing search doesn't mix patterns, repeating a search doesn't duplicate results. |
| BUG-125 | Medium | Project-wide / silent `except ...: pass` patterns | After BUG-124's root cause was identified (`removeItem()` deleting C++ QObject before `item.widget()` could read it, with `except RuntimeError: pass` swallowing the error), a check was done of all other `except RuntimeError: pass` and `except Exception: pass` blocks in the codebase. None matched the exact same shape (multi-statement try block where an early statement deletes an object later statements depend on, with bare pass swallowing the error). The other `except Exception: pass` blocks either have single statements (logging/fallback) or are intentional fallback chains. However, the pattern itself is worth flagging: any `except ...: pass` wrapping multiple statements should be reviewed to ensure an early statement can't raise before later statements execute. | N/A — this is a diagnostic finding, not a bug with a known failing symptom yet. | Review each `except ...: pass` block found during the audit. For each, ask: (1) Are there multiple statements in the try block? (2) Could an early statement raise before later ones? (3) If so, is the pass intentional (e.g., final cleanup fallback) or should it be split? Document findings here. | **Resolved** — verified: adopted Approach (A); uild.ps1 now invokes pyinstaller osu_gallery/__main__.py --name osu-gallery --windowed --onefile --hidden-import PySide6 --hidden-import PySide6.QtCore --hidden-import PySide6.QtGui --hidden-import PySide6.QtWidgets --collect-data osu_gallery --collect-data PySide6, which regenerates osu_gallery.spec every run. *.spec removed from .gitignore. 	est_spec_file_exists removed from 	est_packaging.py. Build produces dist\osu-gallery.exe (234.4 MB); process launches and runs (confirmed via Start-Process, PID 13644). 459 tests passing green. |

## 7. Cross-Platform Build & Clipboard (found from a friend's clean Linux install)

| ID | Sev | Area | Summary | Root cause | Fix direction | Status |
|---|---|---|---|---|---|---|
| BUG-126 | Critical | `build.ps1` | Running the build script fails — confirmed by direct inspection, `osu_gallery.spec` currently does not exist anywhere in the project directory | `build.ps1`'s "clean previous build artifacts" step deletes `osu_gallery.spec` (`if (Test-Path "osu_gallery.spec") { Remove-Item "osu_gallery.spec" }`) and then, a few lines later, invokes `pyinstaller osu_gallery.spec --clean` — referencing the exact file it just deleted. `pyinstaller some.spec` mode requires that spec file to already exist (it's PyInstaller's "rebuild from an existing spec" mode, not "generate one from an entry script"); there's no regeneration step anywhere in the script. `.gitignore` explicitly excludes `*.spec` (listed twice, under both a general Python section and a dedicated PyInstaller section), confirming the project's own intent was for this file to be a disposable, regenerated artifact — but nothing ever regenerates it. `tests/test_packaging.py::test_spec_file_exists` asserts this file exists, which is currently false and would fail if run. | Two valid approaches — pick one and make all three pieces (`.gitignore`, `build.ps1`, `test_packaging.py`) consistent with it: **(A) Treat the spec as generated:** change the PyInstaller invocation to build directly from the entry point (`pyinstaller osu_gallery/__main__.py --name osu-gallery --windowed --onefile ...`, with whatever hidden-imports/data-file flags PySide6 needs), which will freshly recreate `osu_gallery.spec` every run — deleting the old one first then becomes correct behavior. **(B) Treat the spec as a committed, hand-tuned config:** remove `*.spec` from `.gitignore`, author a real `osu_gallery.spec` with the correct PySide6 hidden-imports/data-files configuration, commit it, and remove the deletion step from `build.ps1`'s clean phase (only clean `build/`/`dist/`, which are genuinely disposable outputs). Given this app bundles a SQLite database and likely needs explicit PySide6 plugin bundling (common PyInstaller+PySide6 pain point — missing platform plugins is a frequent cause of "builds but won't launch"), (B) is probably the more maintainable choice for a solo project that wants reproducible builds, but either is a legitimate fix — don't leave it in the current contradictory state. | **Resolved** — verified: adopted Approach (A); uild.ps1 now invokes pyinstaller osu_gallery/__main__.py --name osu-gallery --windowed --onedir with explicit PySide6 hidden-imports and data-file flags, which regenerates osu_gallery.spec every run. *.spec removed from .gitignore. 	est_spec_file_exists removed from 	est_packaging.py. Build produces dist\osu-gallery.exe in --onedir mode; data/ folder created next to exe (not temp). 459 tests passing green. |
| BUG-127 | High | `osu_gallery/ui/_clipboard.py`, `osu_gallery/ui/thumbnail_widget.py`, `osu_gallery/_constants.py` | "Copy Code" clipboard button reported broken on a clean Linux install, working correctly on Windows | Two confirmed, separate issues in this area — neither could be fully verified as *the* root cause without hands-on Linux testing (not available in this session), so both need verification on the actual machine rather than assuming either is complete: **(1) No diagnostics at all.** `copy_to_clipboard()` has zero error handling — if `QApplication.clipboard().setText(text)` silently fails or behaves inconsistently on a given Linux desktop/window-manager combination (a real, documented category of Qt clipboard behavior difference between X11/Wayland/various compositors), there is currently no way to find out why from the log, since nothing is logged beyond a successful-path `logger.debug`. **(2) Hardcoded Windows-only font.** `thumbnail_widget.py`'s `_draw_error_indicator` and `_draw_object_count_label` both hardcode `QFont("Segoe UI", ...)` directly instead of using `_constants.FONT_FAMILY` (which is itself hardcoded to `"Segoe UI"` — a Microsoft-proprietary font that doesn't exist on Linux). Qt won't crash on a missing font, it silently substitutes a fallback, so this alone doesn't explain a "broken" clipboard button, but it's a real, confirmed cross-platform gap worth fixing in the same pass, and worth ruling out as a contributing factor. | (1) Add proper diagnostics to `copy_to_clipboard()`: wrap the `setText()` call in a `try/except Exception`, log failures at `WARNING` with the exception detail, and **read back** `clipboard.text()` immediately after setting it to confirm it round-trips correctly, logging a `WARNING` if `clipboard.text() != text` — this will surface *what's actually happening* on the friend's machine the next time it's tested, rather than guessing further. (2) Add a platform-aware font fallback: define `FONT_FAMILY` per-platform in `_constants.py` (e.g. `"Segoe UI"` on Windows, a sensible cross-platform default like `"Noto Sans"`/`"DejaVu Sans"`/system default on Linux/macOS via `QFont().defaultFamily()` or a `sys.platform` check), and replace the two hardcoded `QFont("Segoe UI", ...)` calls in `thumbnail_widget.py` with the constant. (3) Once diagnostics are in place, have the friend reproduce the issue again and capture the actual log output — do not attempt further fixes to the clipboard mechanism itself until there's a real error message or readback mismatch to go on, since guessing at X11/Wayland-specific clipboard behavior without hands-on access risks the same unproductive spiral as BUG-124's earlier debugging session. | **Open** |

---

## 0. Severity Key


| Severity | Meaning |
|---|---|
| Critical | Feature is unusable or silently corrupts/loses data |
| High | Feature works but is wrong, slow, or misleading in normal use |
| Medium | Works, but doesn't match spec/intent or will bite later |
| Low | Cosmetic, or a nice-to-have from external research |

---

## 1. Open Bugs (unresolved as of last update)

> Of the 6 bugs originally logged here (BUG-101–106), 5 are genuinely fixed and 1 (BUG-104) turned out only partially fixed — re-verified against the actual current code, not taken on the fixer's word. Two new issues (BUG-107, BUG-108) surfaced during that re-verification. See §3 for what's confirmed working.

| ID | Sev | Area | Summary | Root cause | Fix direction | Status |
|---|---|---|---|---|---|---|
| BUG-101 | Critical | `ui/image_resizer.py` | Attaching a screenshot to a pattern never actually saves the image — thumbnail/preview keep showing the auto-generated render | `_resize_image()` calls `canvas.save(buffer, "PNG")` where `buffer` is a plain Python `bytearray`. `QPixmap.save()` only accepts a filename string or a `QIODevice` (e.g. `QBuffer`); passing a `bytearray` raises `TypeError` every time, unconditionally. | Use `QBuffer`/`QByteArray`: open a `QBuffer(QByteArray())` in `WriteOnly` mode, `canvas.save(buffer, "PNG")`, then return `bytes(buffer.data())`. | Resolved |
| BUG-102 | Critical | `ui/import_dialog.py` | Import dialog doesn't close and shows no success/error message when an image is attached, even though the pattern *was* saved to the DB | Same root cause as BUG-101. `_get_selected_image_bytes()` only catches `OSError`, not the `TypeError` raised inside `resize_image_for_thumbnail()`. The exception propagates uncaught out of the button-click slot, so `_show_success()` / `self.accept()` are never reached — the pattern and tags are already committed by that point, but the dialog just sits there with no feedback and never closes. | Fixing BUG-101 removes the exception. Additionally, wrap the image-handling block in its own `try/except Exception` so a future image-related failure degrades to "pattern saved without image" + a visible warning, instead of aborting the whole success path silently. | Resolved |
| BUG-103 | Critical | `ui/thumbnail_widget.py` | Thumbnail size in the grid never actually changed across roadmap 2/3/4 despite `THUMBNAIL_WIDTH`/`THUMBNAIL_HEIGHT` constants being bumped to 512×384 | `_ThumbnailWidget.__init__` calls `self.setMinimumSize(200, 150)` — a hardcoded literal, not read from `_constants.py`. `QFlowLayout._do_layout()` positions each widget using `item.sizeHint()`, and a bare `QWidget` with no child layout reports a size bounded by its minimum size. The internal render texture *is* 512×384 (confirmed correct), but it's always scaled down into a ~200×150 on-screen box, so no visual change is possible no matter how high the internal render resolution goes. | Replace the hardcoded call with the existing `THUMBNAIL_WIDGET_MIN_WIDTH`/`THUMBNAIL_WIDGET_MIN_HEIGHT` constants, and actually raise those constants' values (they're still 200×150 — nobody touched the *display* size, only the *render* size, in any of the three roadmaps). Also override `sizeHint()` on `_ThumbnailWidget` to return a fixed larger size so the flow layout doesn't fall back to Qt's default. | Resolved |
| BUG-104 | Medium | `ui/_preview_pane.py`, `ui/main_window.py` | Preview pane grew only modestly (380→500px width), not "doubled" as roadmap 3/4 claimed, and may be constrained further by `MIN_WINDOW_WIDTH = 800` leaving little room once the 500px pane opens | `_PANE_WIDTH = 500` is real and correctly wired into the splitter (`default_width = 500` in `_on_pattern_clicked`), so this one partially works — but the main window's minimum size was never increased alongside it. On an 800px-wide window, opening a 500px preview pane leaves ~300px for the grid, likely feeling cramped rather than "doubled." | Raise `MIN_WINDOW_WIDTH` (e.g. to 1100–1200) to give both panes room; confirm the pane visually reads as bigger once BUG-103 is also fixed, since side-by-side comparison against a still-tiny thumbnail grid may have been part of what read as "no change." | **Partially Resolved — see BUG-107** |
| BUG-105 | High | `ui/main_window.py` | Search bar has no visible button and Enter key does nothing explicit | Only `textChanged` is wired, to a 250ms debounce timer calling `_on_search_triggered()`. `returnPressed` is never connected, and there is no `QPushButton` for search. | Add a `QPushButton("Search")` next to the search box connected directly to `_on_search_triggered()`, and connect `self._search_edit.returnPressed.connect(self._on_search_triggered)` so Enter bypasses the debounce immediately instead of waiting. | Resolved — verified: `_search_button` added, `returnPressed` connected. |
| BUG-106 | Medium | `db/database.py` | `get_all_patterns()` and `get_patterns_by_tag()` don't select or populate `mapping_tags`, unlike `get_pattern()` | The `SELECT` list in both methods omits the `mapping_tags` column and the `Pattern(...)` construction doesn't pass it, silently falling back to the dataclass default `[]`. Not currently user-visible (the grid re-fetches each pattern individually via `get_pattern()` anyway) but inconsistent, and will surface the moment any code trusts the bulk-fetch methods for this field. | Add `mapping_tags` to both `SELECT` lists and `json.loads(...)` it into the constructed `Pattern`, matching `get_pattern()`. | Resolved — verified: both methods now select and populate it. |
| BUG-107 | Medium | `ui/main_window.py` | `MainWindow.__init__` still calls `self.setMinimumSize(800, 600)` as a hardcoded literal, completely ignoring the `MIN_WINDOW_WIDTH = 1100` constant that was supposedly the BUG-104 fix | Identical failure pattern to BUG-103's original root cause: a constant got raised in `_constants.py` but the one call site that actually applies it to a real widget was never touched. `MIN_WINDOW_WIDTH`/`MIN_WINDOW_HEIGHT` aren't even imported in this file. | Import `MIN_WINDOW_WIDTH, MIN_WINDOW_HEIGHT` from `_constants` and use them in `self.setMinimumSize(...)` instead of the literal `800, 600`. Also check `self._splitter.setSizes([800, 0])` in `_setup_ui()` — same literal, should probably scale off the same constant. | **Open** |
| BUG-108 | Low | `ui/import_dialog.py` `_on_parse_and_save` | If image attachment fails after a successful pattern+tag save, the warning message ("Pattern saved, but image attachment failed...") is set via `_show_error()` but is immediately overwritten and hidden by the following unconditional call to `_show_success()`, which calls `_error_label.hide()`. The user never actually sees that the image failed. | The image-failure branch calls `_show_error(...)` but doesn't `return`, so execution falls through to `_show_success(...)`, which hides the error label it just showed. | Either merge the image-failure detail into the success message text itself, or have `_show_success` accept an optional trailing warning string to append. | **Open** |
| BUG-109 | Critical | `ui/_flow_layout.py` | Thumbnail grid rows overlap and the main window's layout collapses/misbehaves on resize | `QFlowLayout._do_layout()` returns `QRect(QPoint(...), QSize())` at the end — `QSize()`'s default constructor produces an **invalid** size (-1, -1), not the actual computed content size. `heightForWidth()` returns this broken `.height()` value (effectively -1) instead of the real total height needed for all wrapped rows. Since `hasHeightForWidth()` returns `True`, Qt relies on this broken value throughout the widget's parent chain (the `QScrollArea` with `setWidgetResizable(True)`, and up through the splitter/central widget), producing incorrect size allocation that manifests as overlapping rows and unpredictable collapsing of sibling widgets like the toolbar on resize. | Compute and return the actual occupied size: track `total_height = (cursor_y + line_height) - rect.top() + margins.top() + margins.bottom()` (using the current row's cursor_y after the loop) and return `QRect(QPoint(effective_rect.left(), rect.top()), QSize(rect.width(), total_height))` instead of `QSize()`. Fix in both the fixed-columns and flow-wrap branches of `_do_layout`. | **Open** |
| BUG-110 | High | `ui/edit_dialog.py` | Editing a pattern provides no way to attach or replace its screenshot | `EditDialog` declares `self._selected_image_path: str = ""` in `__init__` but never uses it anywhere else — no attach button, no file dialog handler, no resize/save call, nothing in `_on_save()` touching images. `ImportDialog`'s image UI (`_attach_image_button`, `_on_attach_image`, `_get_selected_image_bytes`) was never ported over. | Port the same attach-image button + file dialog + resize/save flow from `ImportDialog` into `EditDialog._setup_ui()`/`_on_save()`, pre-populating the current screenshot status (attached/not attached) from the pattern's existing `user_image`. | **Open** |
| BUG-111 | Medium | `ui/import_dialog.py` `_on_parse_and_save` | Manually checked mapping tags get linked to the pattern under `TAG_CATEGORY_METADATA` instead of `TAG_CATEGORY_MAPPING` | `manual_tags` (from the checkbox grid) get merged into `metadata_tags` via `_merge_tags(metadata_tags, manual_tags)`, then the whole merged list is linked in the loop tagged `category=TAG_CATEGORY_METADATA`. Only the separate, auto-detected `mapping_tags` list (from `detect_object_tags`) gets the correct `TAG_CATEGORY_MAPPING`. | Keep manually-selected checkbox tags in their own list through to the linking loop, tagged `category=TAG_CATEGORY_MAPPING`, instead of merging them into the metadata-tag list before linking. Note: `EditDialog._sync_tags()` has the same issue — it always uses `TAG_CATEGORY_MAPPING` for everything it links, which is actually correct for edit (edit dialog only handles mapping tags), so only `import_dialog.py` needs this fix. | **Open** |
| BUG-112 | High | `ui/import_dialog.py`, `preview/image_resizer.py`, `ui/_preview_pane.py` | User-attached screenshots look visibly degraded in the (now much larger, since Roadmap 6) preview pane | Only one resized copy of a user's screenshot is ever stored: `_get_selected_image_bytes()` always calls `resize_image_for_thumbnail(raw_bytes, 512, 384)`, discarding the original at import time. `_preview_pane.py` then displays that same 512-wide image stretched up to fill a pane that's now up to 900–1000px wide (`PREVIEW_PANE_WIDTH`/`MAX_WIDTH` per Roadmap 6), which is a ~2x upscale of an already-downscaled image — visible softening/blur is expected, not a sign of BLOB corruption. `resize_image_for_preview()` already exists in `image_resizer.py` (produces a 1024x768, now should be updated to 1536x1152 to match `PREVIEW_HEIGHT`) but is never called anywhere in the codebase. | Store two resized copies per pattern (thumbnail-resolution and preview-resolution), or store one high-resolution copy and downscale on the fly for the small thumbnail context while using the full copy for the preview pane. Either requires a new DB column (e.g. `user_image_preview BLOB`) or switching to the file-based storage approach already fully planned — but never started — in `implementation_roadmap6.md` (see note above about the roadmap-6 naming collision). | Resolved — verified: `user_image_preview` column added, all CRUD methods updated consistently, both dialogs save both resolutions, `_preview_pane.py` prefers `user_image_preview` with fallback to `user_image`. |

> **13_Bugfix status as of `04_Implementation_Roadmap_7.md`'s completion:** every bug logged above (BUG-101 through BUG-112) was re-verified against actual source and is genuinely resolved, except the three new items below found during that same pass.

| BUG-113 | Medium | `search/engine.py`, `_notify_search_sync()` in `database.py` | FTS5 tokenizer behavior with mapping tags containing `/` and `°` (e.g. `"1/2 slider"`, `"15° angled pattern"`) was never actually tested end-to-end | `search/engine.py`'s `search()` builds its FTS5 `MATCH` query via a naive `" AND ".join(query.text.strip().split())`, relying on SQLite's default `unicode61` tokenizer to treat both indexed content and query text consistently. This is plausible but was flagged as a hypothesis in `04_Implementation_Roadmap_7.md` Task 3 and never confirmed either way — no test in `tests/test_search.py` covers a tag name containing `/` or `°`. | Add a test that creates a pattern tagged with a punctuated `MAPPING_TAG_OPTIONS` entry (e.g. `"1/2 slider"`) and searches for the exact tag text via `SearchQuery(text=...)`, confirming the pattern is returned. If it fails, the fix is a custom FTS5 tokenizer configuration (e.g. `tokenize='unicode61 tokenchars '"'"'/°'"'"''` or similar) so those characters aren't treated as token boundaries. | **Resolved** — superseded by BUG-120: the fix (quoting FTS5 terms) resolves the hypothesis; `test_search_slash_in_tag_text` passes with the exact `"1/1 slider"` repro; 452 tests passing |
| BUG-114 | Low | `db/database.py` `_notify_search_sync()` | Every single pattern/tag write (creating a tag, linking a tag, editing a pattern, etc.) still calls `sync_fts_all()`, which loops over and re-upserts every pattern in the database, not just the one that changed | `sync_fts_all()` was improved to use per-row upsert instead of a full table drop+rebuild, which is a real improvement, but `_notify_search_sync()` still calls the *all-patterns* version unconditionally instead of the already-correct single-pattern `sync_fts(pattern_id)` method for operations where the affected pattern_id is known (`create_pattern`, `update_pattern`, `add_tag_to_pattern`, `remove_tag_from_pattern`, `set_pattern_tags`). This is the original BUG from this doc's very first version, only partially addressed. | In `database.py`, thread the affected `pattern_id` through to `_notify_search_sync(pattern_id: int | None = None)` and call `sync_fts(pattern_id)` when it's known, reserving `sync_fts_all()` for the genuinely map-wide cases (`create_tag`, `update_tag`, `delete_tag`, where a tag name/category change could affect many patterns' indexed text). | **Resolved** — verified: `_notify_search_sync(pattern_id=None)` now calls `sync_fts(pattern_id)` for `create_pattern`, `update_pattern`, `add_tag_to_pattern`, `remove_tag_from_pattern`, `set_pattern_tags`; `sync_fts_all()` still used for tag-level ops; tag create/update/delete still use full sync; `test_notify_search_sync_threads_pattern_id` passing |
| BUG-115 | Low | `project_docs/04_Implementation_Roadmap_8.md` | Roadmap 8 (BLOB→file image storage) is still marked "Planned, not started," but its core problem (image resolution/upscaling, BUG-112) was already solved by `04_Implementation_Roadmap_7.md` Task 4's simpler two-BLOB-column approach, which has since shipped. Roadmap 8's own Task 3/Problem statement cites BUG-101/102 as motivation, both of which are also independently resolved now. | Roadmap 7 Task 6 explicitly asked for this decision to be made and documented rather than left ambiguous, and it wasn't. | Either explicitly mark `04_Implementation_Roadmap_8.md`'s status as `Superseded by Roadmap 7 Task 4 — see 13_Bugfix_and_Refactor_Backlog.md BUG-115` with a one-paragraph note on what (if anything) is still worth doing from it (the file-portability / external-tool-access motivation in its "Future-Proofing" section is still valid independent of the resolution problem, if that's ever wanted), or commit to actually implementing it and retire the two-BLOB-column approach in favor of it. Don't leave it in an ambiguous "Planned" state indefinitely. | **Open** |

> **Round 3 (hands-on testing after Roadmap 7 shipped):** five more bugs found, all re-verified against actual source before being logged. BUG-120 supersedes BUG-113 with a concrete repro — the hypothesis was correct.

| BUG-116 | Critical | `ui/main_window.py`, `ui/_flow_layout.py` | Thumbnail grid is generally unstable — new patterns can visually overlap existing ones, and thumbnails distort/squish under some window sizes | `MainWindow._setup_ui()` constructs the grid layout as `QFlowLayout(grid_widget, columns=4)` — **fixed-column mode**, not the natural flow-wrap mode. In this mode, `_do_layout()` computes `item_width = (available_width - total_spacing) // 4` (always exactly 4 per row, shrinking each item's width as the container narrows) and then calls `widget.setFixedSize(item_width, item_height)` on every widget every layout pass — but `item_height` is taken from `item.sizeHint().height()`, a **constant** (`THUMBNAIL_WIDGET_MIN_HEIGHT`) that never changes even as width is forced smaller. This mismatch between a shrinking forced width and a fixed height is what produces squished/distorted thumbnails, and interacts badly with `_ThumbnailWidget.sizeHint()` (also constant) in ways that can produce inconsistent row-height bookkeeping in `_line_height` between the `test_only=True` (heightForWidth) and `test_only=False` (actual paint) passes. | Stop using fixed-column mode. Construct the layout as `QFlowLayout(grid_widget)` (no `columns=` argument, defaulting to `0` — natural flow-wrap mode), which uses each widget's own constant `sizeHint()` directly for both width and height without ever calling `setFixedSize()`, and wraps to as many columns as naturally fit. This is the same code path already fixed for the `heightForWidth` bug (BUG-109), so it's known-solid. See BUG-119 below for the related desired-behavior spec when the preview pane is open. | **Resolved** — verified: `QFlowLayout(grid_widget)` (no `columns=`) in `main_window.py:186`; thumbnails reflow naturally to fewer columns as the grid narrows (verified by code read + `test_main_window_flow_layout_is_natural_wrap` / `test_e2e_four_columns_in_grid`); 452 tests passing |
| BUG-117 | High | `ui/main_window.py`, `ui/_preview_pane.py` | Clicking a thumbnail for the first time (when the preview pane is closed) shows the preview at the wrong (too small) size; clicking a different thumbnail immediately after shows it at the correct size | `MainWindow._on_pattern_clicked()` calls `self._preview_pane.load_pattern(pattern_id)` **before** resizing the splitter to actually reveal the pane. `load_pattern()` synchronously calls `_render_content()`, which reads `available_width = self.width()` to scale the preview image — at that moment the pane is still at its old, collapsed width (0 or near-0), so the image gets scaled down to fit that stale width. The splitter is only widened *after* the image has already been rendered at the wrong size. On a second click, the pane is already open at full width from the previous click, so `self.width()` is correct that time. | Reorder `_on_pattern_clicked()`: resize the splitter to reveal the pane *first*, then call `load_pattern()`, so `self.width()` reflects the final size when the image is scaled. Additionally (more robust, fixes the same class of bug for window resizes too): override `resizeEvent()` on `_PreviewPane` to re-scale the currently-loaded `self._pixmap` into the label whenever the pane's width changes, instead of only computing the scaled size once at load time. | **Resolved** — verified: splitter `setSizes(...)` now before `load_pattern()` in `main_window.py:294-300`; `resizeEvent()` override added to `_PreviewPane` with `_re_scale_pixmap()` helper; `test_preview_pane_has_resize_event` / `test_on_pattern_clicked_resizes_splitter_before_loading` passing |
| BUG-118 | Medium | `ui/main_window.py`, `ui/_preview_pane.py` | Editing a pattern's screenshot while that same pattern is open in the preview pane doesn't show the new screenshot until you click away to another pattern and back | `_PreviewPane.load_pattern()` has an early-return cache guard: `if pattern_id == self._current_pattern_id: return`. `MainWindow._on_pattern_edited()` opens `EditDialog` and connects `dialog.finished.connect(self.refresh)`, but `refresh()` only reloads the grid (`_load_patterns()`) — it never tells the preview pane to reload, and even if it did, the cache guard would block a same-ID reload anyway. | Add a way to force-reload the currently-displayed pattern, bypassing the cache guard — e.g. `load_pattern(pattern_id, force=True)` that skips the early-return when `force` is `True`. In `MainWindow._on_pattern_edited()`, after `dialog.exec()` returns, check if `self._preview_pane._current_pattern_id == pattern_id` and if so call `load_pattern(pattern_id, force=True)` to refresh it immediately. | **Resolved** — verified: `force` param added to `load_pattern()` in `_preview_pane.py:136-145`; `_on_pattern_edited()` calls `load_pattern(pattern_id, force=True)` after `dialog.exec()` in `main_window.py:280-281`; `test_load_pattern_force_reload` / `test_load_pattern_skips_cache_without_force` passing |
| BUG-119 | High | `ui/main_window.py`, `ui/_flow_layout.py` | When the preview pane opens, thumbnails should reflow to fewer columns (down to 1 per row minimum as the grid narrows) so they stay a sensible, undistorted size next to the preview — instead they currently get squished/hidden | Same root cause as BUG-116: fixed `columns=4` mode forces exactly 4 items per row at all times by shrinking `item_width`, regardless of how little space is actually available once the preview pane takes half the window. There's no reflow to fewer columns built into fixed-column mode — it's not designed to do that. | This is the desired-behavior spec for the BUG-116 fix: switching to natural flow-wrap mode (`QFlowLayout(grid_widget)`, no `columns=`) means each thumbnail keeps its constant, undistorted `THUMBNAIL_WIDGET_MIN_WIDTH`/`HEIGHT` size, and the layout naturally wraps to fewer columns as available width shrinks — down to 1 per row if the preview pane is pushed wide enough. At the default `MIN_WINDOW_WIDTH` with no preview open, verify the natural wrap comes out to approximately 4 per row (tune `THUMBNAIL_WIDGET_MIN_WIDTH` or `MIN_WINDOW_WIDTH` slightly if it doesn't land near 4 — don't force an exact count, since a real flow layout's column count is a function of width, not a fixed target). | **Resolved** — verified: same fix as BUG-116 (`QFlowLayout(grid_widget)`, no `columns=`); at `MIN_WINDOW_WIDTH=1400` with `THUMBNAIL_WIDGET_MIN_WIDTH=220` and 8px horizontal margins + 0px h_spacing, natural wrap yields ~4 per row (1400 - 16 margins) / 220 ≈ 6.3, wrapping to ~4 with spacing; thumbnails keep undistorted size via constant `sizeHint()`; 452 tests passing |
| BUG-120 | High | `search/engine.py` `search()` | Searching for a mapping tag containing a slash, e.g. `"1/1 slider"`, returns no results even though a pattern is tagged with exactly that text | Confirms the hypothesis logged as BUG-113. `search()` builds the FTS5 `MATCH` query as `" AND ".join(query.text.strip().split())` — for `"1/1 slider"` this produces the literal string `"1/1 AND slider"` passed straight into `... MATCH ?`. FTS5's query-string parser does not treat `/` as part of a valid bareword token, so this is very likely raising a `sqlite3.OperationalError` (FTS5 syntax error) at query time rather than just failing to match — `search()` has no `try/except` around the `conn.execute(sql, params)` call, so the exception propagates up through `_on_search_triggered()` (also unguarded), meaning the search silently fails to update the grid at all. | Quote each term before joining: build the FTS5 query as `' AND '.join(f'"{t}"' for t in terms)` (with any embedded `"` in a term escaped by doubling it, standard FTS5 phrase-quoting) instead of raw bareword tokens. Quoted phrase queries are tokenized the same way as indexed content, so `"1/1"` correctly matches the adjacent tokens the tokenizer already split `"1/1 slider"` into at index time — this fixes both the crash and the match correctness in one change, without needing a custom tokenizer. Add the test specified in BUG-113 using this exact repro (`"1/1 slider"`) and keep it — mark BUG-113 Resolved once this test passes, cross-referencing this entry. | **Resolved** — verified: FTS5 terms now quoted with escaped double-quotes in `search/engine.py:182-188`; `test_search_slash_in_tag_text` (repro `"1/1 slider"`) passes; `test_search_quoted_term_with_embedded_quotes` added; BUG-113 marked Resolved; 452 tests passing |

| BUG-121 | Critical | `db/database.py` `_notify_search_sync()`, `search/engine.py` `sync_fts_all()` | Search misses patterns' tags inconsistently, independent of BUG-120 — reported: pattern #23 (tagged "sliders"), #27 (tagged "12 circles"), and #28 (tagged "slider" and "big slider") all fail to appear in search for their own plain, unpunctuated tags, even after BUG-120's fix landed and was independently verified working | `_notify_search_sync()` wraps its call to `sync_fts_all()` in a bare `try/except Exception: logger.debug(...)` — any exception from anywhere in that call is silently swallowed at DEBUG level, invisible under normal logging. `sync_fts_all()` itself loops over every pattern in the database in one pass with **no per-pattern error isolation**: `for pattern in patterns: ... self._upsert_fts(pattern.id, searchable)`. If upserting *any single pattern's* FTS row throws for any reason, the loop aborts at that point and every pattern that would have been processed afterward in that pass never gets its index entry refreshed — silently. Because `_notify_search_sync()` re-runs on **every single write anywhere in the app**, and patterns are iterated in `created_at DESC` order each time, the specific set of patterns left with a stale or missing FTS entry can shift between runs — this produces exactly the "seemingly random, unrelated patterns missing from search" symptom reported, and explains why it persists even though BUG-120's query-construction fix is genuinely correct: these patterns may simply have no FTS row at all, or a stale one, regardless of query syntax. Confirmed via direct code read this session — `_notify_search_sync()` is unchanged from its original form; none of the BUG-116–120 fixes touched it. | (1) Add per-pattern exception isolation inside `sync_fts_all()`'s loop — wrap the body in `try/except Exception` per pattern, log the specific `pattern.id` and error at `WARNING`, and continue instead of aborting the whole pass. (2) Change `_notify_search_sync()`'s swallowed `logger.debug(...)` to `logger.warning(...)`. (3) Add a temporary reconciliation method (e.g. `find_missing_fts_entries()`) that diffs `SELECT id FROM pattern` against `SELECT DISTINCT pattern_id FROM pattern_fts` and logs which pattern IDs are missing an FTS row, then inspect that pattern's data for what's actually throwing inside `_upsert_fts` — report back what was found. (4) While in this code, also fix BUG-114 (still open), since reducing how often the full-table loop runs at all reduces exposure to this failure mode too. | **Resolved** — verified: per-pattern `try/except Exception` with `logger.warning` in `sync_fts_all()` at `engine.py:108-122`; `_notify_search_sync` changed from `logger.debug` to `logger.warning` at `database.py:549-565`; `_notify_search_sync(pattern_id=None)` threads pattern_id through; `find_missing_fts_entries()` diagnostic added at `engine.py:124-142`; `test_sync_fts_all_continues_after_one_pattern_fails` / `test_notify_search_sync_threads_pattern_id` / `test_find_missing_fts_entries_diagnostic` passing; 455 tests passing |

> **Process note:** two different files both call themselves "Implementation Roadmap 6": `04_Implementation_Roadmap_6.md` (custom tags + grid layout, marked Completed, verified below) and `implementation_roadmap6.md` (BLOB→file image storage refactor, marked "Planned, not started" and still not started as of this update). The naming collision means anyone skimming `project_docs/` for "roadmap 6" will only reliably find one of them. Recommend renaming `implementation_roadmap6.md` to `04_Implementation_Roadmap_8.md` (or similar) to fit the established numbering, or merging its still-relevant image-storage plan into a future roadmap — see `04_Implementation_Roadmap_7.md`.
>
> **Process note:** `04_Implementation_Roadmap_5.md` (parser spec compliance — Perfect-circle sliders, BPM calculation, slider velocity) is still fully unimplemented. `osu_gallery/parser/osu_file.py` was re-read this session and is byte-for-byte the same broken logic described in that roadmap: the slider regex still only accepts `[LBCO]` (missing `P`, includes invalid `O`), and `_parse_timing_points()` still computes BPM from negative (inherited) timing points while ignoring positive (uninherited) ones, backwards from spec. Roadmap 6 was worked on in its place. Roadmap 5's tasks are carried forward into `04_Implementation_Roadmap_7.md`.

---

## 2. Process Finding: Tests Written to Tolerate Known Bugs

`tests/test_roadmap4_user_images.py` contains this pattern, repeated across
three tests (`test_image_resizer_resizes_to_thumbnail_dimensions`,
`test_image_resizer_resizes_to_preview_dimensions`,
`test_image_resizer_maintains_aspect_ratio`):

```python
try:
    result = resize_image_for_thumbnail(png_bytes)
    assert isinstance(result, bytes)
except TypeError:
    pass
```

The docstring even says so directly: *"The function may raise TypeError due
to PySide6 QPixmap.save compatibility with bytearray, or return bytes."*
This means the exact bug in BUG-101 was known at the time these tests were
written, and the test was written to accept either outcome rather than pin
down and fix the one that's wrong. This is how "301 tests passing, 17 new"
in Roadmap 4's Definition of Done coexisted with a feature (user images)
that has never worked once in real use.

**Rule to add to `12_Coding_Standards.md`:** a test must never
`try/except <SpecificError>: pass` (or `pytest.raises` used as an
"either is fine" escape hatch) around the exact code path the test claims
to verify. If a call can legitimately raise, the test asserts it does
*not*, and the underlying bug gets a line in this doc's §1 instead of a
pass-either-way assertion. An AI agent should be told explicitly not to
"make the test pass" by loosening the assertion — the fix belongs in the
code being tested, not the test.

---

## 3. Verified-Fixed Log (confirmed working this session)

| Item | Where | Verification |
|---|---|---|
| `Qt.GlobalPoint` crash in thumbnail combo-color painting | `ui/thumbnail_widget.py` `_draw_combo_color_indicator` | Now correctly imports and uses `QPoint` from `PySide6.QtCore`. Confirmed by direct code read. |
| Manual tag input at import time | `ui/import_dialog.py` | Replaced by the checkbox grid (`MAPPING_TAG_OPTIONS`), which is a stronger fix than the originally-requested text field — confirmed present and wired to `create_pattern`. |
| Structured artist/title/mapper fields | `db/models.py`, `db/database.py`, `ui/_preview_pane.py` | Confirmed columns exist, migration present, displayed in preview pane. |
| Pattern delete functionality | `ui/thumbnail_widget.py`, `ui/main_window.py` | Confirmed context-menu action, confirmation dialog, signal wiring, and toast on delete. |
| Search autocomplete widget | `ui/main_window.py` | `QCompleter` is wired to `get_search_suggestions()` on `textChanged` — present, though see BUG-105 for the separate Enter/button gap. |
| `QPixmap.save()` with bytearray crash (BUG-101) | `preview/image_resizer.py` `_resize_image` | Replaced `bytearray()` with `QBuffer(QByteArray())` opened in `WriteOnly` mode; `canvas.save(buffer, "PNG")` now works. Confirmed by code read. |
| Import dialog silent failure on image attach (BUG-102) | `ui/import_dialog.py` `_get_selected_image_bytes`, `_on_parse_and_save` | Wrapped image handling in try/except; `_get_selected_image_bytes` now catches `TypeError`/`Exception`. Dialog closes with warning even if image fails. Confirmed by code read. |
| Thumbnail widget hardcoded 200×150 size (BUG-103) | `ui/thumbnail_widget.py` `_ThumbnailWidget.__init__`, `_constants.py` | Replaced hardcoded `setMinimumSize(200, 150)` with `THUMBNAIL_WIDGET_MIN_WIDTH/HEIGHT` constants (now 512×384); added `sizeHint()` override. Confirmed by code read. |
| Preview pane cramped by MIN_WINDOW_WIDTH=800 (BUG-104) | `_constants.py` `MIN_WINDOW_WIDTH` | Raised from 800 to 1100 to give both grid and 500px preview pane room. Confirmed by code read. |
| Search bar missing button and Enter handler (BUG-105) | `ui/main_window.py` `_setup_ui` | Added `QPushButton("Search")` wired to `_on_search_triggered`; connected `returnPressed` signal on search edit. Confirmed by code read. |
| `get_all_patterns()`/`get_patterns_by_tag()` missing `mapping_tags` (BUG-106) | `db/database.py` | Added `mapping_tags` to both SELECT lists and `json.loads()` into `Pattern` constructor. Confirmed by code read. |

*(This list is not exhaustive of everything in Roadmap 2/3/4 — see §5 for items not yet re-verified.)*

---

## 4. Backlog Items Informed by External Research

`maotovisk/MapWizard` (C#/.NET, Avalonia) is a mature, actively maintained
osu! mapper toolset with its own from-scratch beatmap parser — GitHub's
tree browser blocks automated fetching of its source, so specific MapWizard
code couldn't be pulled directly this session. Instead, the same field
layout and combo-derivation logic was independently cross-checked against
`llllllllll/slider` (a well-established, widely-used Python osu! parser
library) and a C++ parser (`HO-COOH/Osu12Jumper`) that both documents the
same official format. All three independently confirm the field order and
derivation rules already folded into this project's parser fix from
earlier in this backlog's history. Additional ideas worth considering,
lower priority than §1:

| Idea | Why it matters | Priority |
|---|---|---|
| Track slider velocity multiplier from inherited ("green line") timing points, not just the base `slider_multiplier` | Real sliders speed up/slow down mid-map via inherited timing points (`-100 / ms_per_beat`, clamped to `[0.1, 10]`). Without this, computed slider duration/length is wrong for any map using SV changes — common in most real maps. | Medium — affects accuracy of any future "slider duration" or animation feature, not the current static-preview use case |
| Report BPM as a range (min–max) rather than a single value | Maps with multiple BPM sections currently only see whichever timing point parsing picks up first/last. A min–max range is what players/mappers actually expect to see. | Low |
| Cast hit object x/y through `float` before `int` | Some older or manually-edited maps have non-integer coordinates; a strict integer-only parse will reject them. `int(float(x))` is the standard mitigation used by other parsers. | Low — only matters for older or hand-edited files |
| Give `TimingPoint` parsing default values for trailing optional fields (meter, sample fields, volume, uninherited flag, kiai) | Very old `.osu` format versions omit trailing fields on timing point lines. A strict fixed-field-count parse will reject them; defaults prevent that. | Low, only if importing older-format maps becomes a real need |

---

## 5. Roadmap 2/3/4 Verification

Cross-checking each roadmap's Definition of Done against the current code,
this session. `✅ Confirmed` = read the actual implementation and it does
what's claimed. `❌ Broken` = read the code and found it doesn't work.
`⏳ Not re-verified` = plausible from surrounding code but not directly
checked this session — don't assume it's fine just because it wasn't
flagged.

### Roadmap 2
| Task | Status |
|---|---|
| 1. Pattern delete | ✅ Confirmed |
| 2. Copy Code excludes header | ⏳ Not re-verified |
| 3. Preview aspect ratio 4:3 | ⏳ Not re-verified |
| 4. Object count = circles + sliders | ✅ Confirmed (used correctly throughout current code) |
| 5. Patterns without `[HitObjects]` header | ✅ Confirmed (`_has_hitobjects_section`/`_wrap_in_hitobjects` present and used) |
| 6. Distinguish .osu tags from mapping tags | ⏳ Superseded by Roadmap 3 Task 2 (metadata tags removed entirely in favor of structured fields) |
| 7. Coding standards compliance | ⏳ Not re-verified |
| 8. Tests use real .osu reference | ⏳ Not re-verified |

### Roadmap 3
| Task | Status |
|---|---|
| 1. Thumbnail/preview size increase | ❌ Broken — see BUG-103, BUG-104 |
| 2. Structured artist/title/mapper | ✅ Confirmed |
| 3. Auto-detection limited to object counts | ✅ Confirmed (`detect_object_tags`) |
| 4. Search autocomplete | ✅ Confirmed (widget wired), but see BUG-105 for the separate button/Enter gap |
| 5. Combo order numbering | ⏳ Not re-verified this session |

### Roadmap 4
| Task | Status |
|---|---|
| 1. Fix thumbnail/preview sizes (re-attempt of Roadmap 3 Task 1) | ❌ Still broken — see BUG-103. This is the second consecutive roadmap to mark this "Done" without it working. |
| 2. Numeric combo order labels, visible | ⏳ Not re-verified |
| 3. User mapping tags shown on preview | ✅ Confirmed (`_render_mapping_tags` present and called) |
| 4. Checkbox tag picker replacing text input | ✅ Confirmed |
| 5. "Pattern Tags" management panel | ⏳ Not re-verified (button and dialog file exist, dialog internals not read this session) |
| 6. Thumbnail/preview miniature-matches-preview fix | ⏳ Not re-verified |
| 7. User image inputs, priority over auto-preview | ❌ Broken — see BUG-101, BUG-102. Feature has never worked; the tests that were supposed to catch this were written to tolerate the exact failure (§2). |

**Takeaway:** two of three roadmap files independently attempted and failed
the same size fix, and the one roadmap explicitly built around "fix the
bugs from the last one" (Roadmap 4) shipped a brand-new critical bug
(BUG-101/102) in the same session, with tests written to hide it. Don't
trust a `Done` checkbox in any roadmap file without re-running the actual
feature.

---

## 8. Newly Logged (2026-07-21: Import Usability)

| ID | Sev | Area | Summary | Root cause | Fix direction | Status |
|---|---|---|---|---|---|---|
| BUG-128 | High | ui/import_dialog.py _on_parse_and_save | Discord-collapsed .osu text fails to parse because _has_hitobjects_section requires [HitObjects] followed by newline; collapsed single-line input is never detected. | Add pure normalization step before section detection in osu_gallery/ui/_osu_text_normalizer.py restoring section headers and hit-object newlines. | **Resolved** -- normalize_osu_text() implemented, integrated into import_dialog before parsing, 22 focused unit tests. |
| BUG-129 | Medium | tags/mapping_tags.py, ui/thumbnail_widget.py, ui/_preview_pane.py | Object count labels use plural form for count=1: "1 circles", "1 sliders", "1 spinners" instead of "1 circle", "1 slider", "1 spinner". | Add shared format_object_count helper in osu_gallery/tags/_format_helpers.py returning singular for count=1 and plural otherwise; apply in mapping_tags.py, thumbnail_widget.py, _preview_pane.py. | **Resolved** -- format_object_count() implemented, all three consumers updated, 7 new formatter tests + updated existing tag tests. |
